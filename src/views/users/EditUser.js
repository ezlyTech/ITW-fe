import React from 'react';
import {Container, Table, Button, Input} from 'reactstrap';

const EditUser = () => {
    return (
		<Container>
            <div>
                <h1>Create User</h1>
            </div>
		</Container>
    );
}

export default EditUser;